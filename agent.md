1. 在验证方法正确性时，应以 1e-6 精度和 .\build\Release\gst_dpbf_main.exe Toronto 得到的结果（result\Toronto\DPBF\default\weights.txt）逐询问相同。结果已经得到，你不需要重新跑；同时还需要过一些随机生成的小数据。

2. 在核对理论时间复杂度时，由于常数等原因，应将 priority_queue 实现的堆视为和斐波那契堆时间复杂度一致，并在具体实现时使用它。

3. 方法不能超过时间复杂度理论上界。

4. 所有的 result 都是增量构建的，一个 weight.txt 可能存储了多个全部或部分运行的结果，在实际比较时应该比较最后的运行结果。

5. Cmake stamp 权限可能不足，记得提权构建。

6. 做完运行需要清理临时文件。

7. 关于长期目标，给你如下一些建议：
(1) best 很可能难以用常规方法降低太多，最优解很可能仅比初始的贪心解劣 10% 左右；
(2) 不妨接受一些预处理时间长但确实实际减少了状态的策略，目前几乎只测试了一条询问的探针，也许其他询问效果不错；
(3) pair 层难以优化不妨先跑满，看看后续层的优化前景，用整体的视角看待。如果等待时间比较长，不妨将代码拆成多层输入输出，例如 pair 层，tuple 层进行；或者先测 DBLP 13 or 14.
(4) 瞎猜 + 验证的命中率比较低，改为 输出探测信息+分析 或者 探究理论必要条件+验证 提高命中率。