1. 在验证方法正确性时，应以 1e-6 精度和 .\build\Release\gst_dpbf_main.exe Toronto 得到的结果（result\Toronto\DPBF\default\weights.txt）逐询问相同。结果已经得到，你不需要重新跑；同时还需要过一些随机生成的小数据。

2. 在核对理论时间复杂度时，由于常数等原因，应将 priority_queue 实现的堆视为和斐波那契堆时间复杂度一致，并在具体实现时使用它；方法不能超过时间复杂度理论上界。

3. 所有的 result 都是增量构建的，一个 weight.txt 可能存储了多个全部或部分运行的结果，在实际比较时应该比较最后的运行结果。

4. Cmake stamp 权限可能不足，先提权构建；做完运行需要清理临时文件。

5. 瞎猜 + 验证的命中率比较低，改为 输出探测信息+分析 或者 探究理论必要条件+验证 提高命中率。

6. 禁止在策略中采用没有道理的超参数和针对某个时刻的特判操作，时刻记住最终目标是产出论文而不是工程性的针对数据。后续不太建议对图/询问本身做其他文献有过的压缩操作，因为 baseline 也可以做，除非是本方法才能使用的或者是原创的。